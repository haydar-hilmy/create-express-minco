<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= error.status || 500 %> | <%= message %></title>
    <link rel="stylesheet" href="/css/tailwind/style.css" />
  </head>
  <body class="min-h-screen bg-gray-100 flex items-center justify-center p-6">
    <div class="max-w-xl w-full bg-white shadow-lg rounded-lg p-8 text-center">
      <div class="text-red-500 text-6xl font-bold mb-4"><%= error.status || 500 %></div>
      <h1 class="text-2xl font-semibold text-gray-800 mb-2"><%= message %></h1>
      <p class="text-gray-600 mb-6">
        Sorry, something went wrong. Please try again later or contact support.
      </p>

      <% if (error.stack && env === 'development') { %>
        <details class="text-left bg-gray-100 rounded p-4 text-sm text-gray-700 overflow-auto max-h-64">
          <summary class="cursor-pointer font-medium mb-2">Error Stack</summary>
          <pre><%= error.stack %></pre>
        </details>
      <% } %>

      <a href="/" class="mt-6 inline-block bg-blue-600 hover:bg-blue-700 text-white font-semibold py-2 px-4 rounded transition">
        ← Back to Home
      </a>
    </div>
  </body>
</html>
