<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <title><%= error.status || 500 %> | <%= message %></title>
    <link rel="stylesheet" href="/css/tailwind/style.css" />
    <script src="https://unpkg.com/@phosphor-icons/web"></script> <!-- Optional icon pack -->
  </head>
  <body class="min-h-screen bg-gradient-to-br from-gray-900 via-gray-800 to-gray-900 flex items-center justify-center px-4 text-white">

    <div class="bg-gray-950/80 shadow-xl rounded-2xl p-10 max-w-xl w-full text-center animate-fade-in">
      <!-- Icon -->
      <div class="text-red-500 text-7xl mb-4">
        <i class="ph ph-warning-circle"></i>
      </div>

      <!-- Error Status -->
      <div class="text-4xl font-bold mb-2"><%= error.status || 500 %></div>

      <!-- Message -->
      <h1 class="text-xl md:text-2xl font-semibold text-gray-200 mb-3"><%= message %></h1>
      
      <p class="text-gray-400 mb-6">
        Sorry, something went wrong. Please try again later or contact support.
      </p>

      <!-- Stacktrace for dev -->
      <% if (error.stack && env === 'development') { %>
        <details class="text-left bg-gray-800/80 rounded p-4 text-sm text-gray-300 overflow-auto max-h-64">
          <summary class="cursor-pointer font-medium mb-2">Show Stack Trace</summary>
          <pre class="whitespace-pre-wrap"><%= error.stack %></pre>
        </details>
      <% } %>

      <!-- Button -->
      <a href="/" class="mt-6 inline-block bg-indigo-600 hover:bg-indigo-700 text-white font-semibold py-2 px-5 rounded-lg transition">
        ← Back to Home
      </a>
    </div>

    <style>
      @keyframes fade-in {
        0% { opacity: 0; transform: translateY(20px); }
        100% { opacity: 1; transform: translateY(0); }
      }

      .animate-fade-in {
        animation: fade-in 0.6s ease-out forwards;
      }
    </style>
  </body>
</html>
